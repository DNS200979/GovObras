{
  "name": "carbonfree-obras",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev:gov": "turbo run dev --filter=@carbonfree/gov",
    "dev:obra": "turbo run dev --filter=@carbonfree/obra",
    "dev:concreteira": "turbo run dev --filter=@carbonfree/concreteira",
    "dev:fiscal": "turbo run dev --filter=@carbonfree/fiscal"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  }
}
